# legacy-bundle

These files were extracted from the `moduleSources` object inside the compacted dashboard.
They are kept together so their original relative imports such as `./app-data.js` still make sense.

Use this folder for the first behavior-preserving migration before moving code into feature folders.
